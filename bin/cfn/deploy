#!/usr/bin/bash

ABS_PATH=$(readlink -f "$0")
CFN_PATH=$(dirname $ABS_PATH)
BIN_PATH=$(dirname $CFN_PATH)

# source "$CFN_PATH/networking"

# source "$CFN_PATH/cluster"
# source "$CFN_PATH/db"
# source "$CFN_PATH/cicd"
# source "$CFN_PATH/cognito"
# source "$CFN_PATH/ddb"

# source "$CFN_PATH/service"

# source "$CFN_PATH/frontend"
# source "$CFN_PATH/sync"
# source "$CFN_PATH/machineuser"

# source "$BIN_PATH/frontend/static-build"
ruby "$BIN_PATH/frontend/sync"